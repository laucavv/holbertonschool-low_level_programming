#include "holberton.h"
/**
 *add -  function that adds two integers and returns the result.
 *@c: values
 *@n: values
 * Return: suma
 */

int add(int c, int n)
{
	int suma;

	suma = c + n;
	return (suma);
}

